'use client';
import Link from 'next/link';
import { useState } from 'react';
export function Login({configured}:{configured:boolean}) {
 const [password,setPassword]=useState('');const [visible,setVisible]=useState(false);const [pending,setPending]=useState(false);const [error,setError]=useState('');
 return <div className="demo-login"><Link className="wordmark" href="/">basic<span>mails</span><i>.</i></Link><div className="login-grid"><div className="login-story"><span className="eyebrow">YOUR INBOX, REIMAGINED</span><h1>A little space.<br/><span className="serif">Just for you.</span></h1><p>A working mailbox demo.<br/>Read, organise, write. Take your time.</p><div className="login-detail"><span>01</span><p>Fictional conversations.<br/>Real interactions.</p></div><div className="login-detail"><span>02</span><p>Your changes stay in this browser.<br/>No emails are sent.</p></div></div><section className="login-card"><span className="eyebrow">PRIVATE DEMO</span><h2>Welcome inside.</h2><p>Enter the access password to explore Basic Mails.</p><form onSubmit={async e=>{e.preventDefault();setPending(true);setError('');try{const res=await fetch('/api/demo/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const data=await res.json();if(!res.ok)throw new Error(data.error||'Unable to sign in.');window.location.assign('/demo');}catch(err){setError(err instanceof Error?err.message:'Unable to sign in.');setPending(false);}}}><label htmlFor="demo-password">Access password</label><div className="password-field"><input id="demo-password" type={visible?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required maxLength={512} disabled={!configured||pending}/><button type="button" onClick={()=>setVisible(!visible)} aria-label={visible?'Hide password':'Show password'}>{visible?'Hide':'Show'}</button></div>{!configured&&<p className="demo-form-error" role="status">Demo access is not configured yet. Please check back soon.</p>}{error&&<p className="demo-form-error" role="alert">{error}</p>}<button type="submit" className="button dark-button" disabled={!configured||pending}>{pending?'Signing in…':'Open your mailbox'}</button></form><div className="login-foot">Private access. No account required.</div></section></div><footer>Basic Mails · Built by Benjamin & Zeno</footer></div>;
}
